<section
  id="contact"
  class="contact section"
  aria-labelledby="contact-title"
>
  <div class="contact-atmosphere" aria-hidden="true"></div>

  <div class="container-wide contact-container">
    <div class="contact-copy">
      <p class="contact-eyebrow">CONTACT</p>

      <h2 id="contact-title">
        From Gozo
        <span>to the world.</span>
      </h2>

      <p class="contact-lead">
        Aleph Innovation is open to technical collaboration, research
        conversations, project ideas and support for independent R&amp;D.
      </p>

      <div class="contact-status">
        <span class="contact-status-dot" aria-hidden="true"></span>
        <span>PRE-INCORPORATION R&amp;D VENTURE · GOZO, MALTA</span>
      </div>
    </div>

    <div class="contact-card">
      <div class="contact-card-topline">
        <span>GENERAL ENQUIRIES</span>
        <span>01</span>
      </div>

      <a
        class="contact-email"
        href="mailto:info@alephinnovation.com"
      >
        info@alephinnovation.com
        <span aria-hidden="true">↗</span>
      </a>

      <p>
        For technical collaboration, project proposals, research participation
        and general enquiries.
      </p>

      <div class="contact-links">
        <a
          href="https://www.linkedin.com/company/alephinnovation/"
          target="_blank"
          rel="noopener noreferrer"
        >
          LinkedIn
          <span aria-hidden="true">↗</span>
        </a>

        <a
          href="https://github.com/lupoleo/agentic_portfolio_stage2_5"
          target="_blank"
          rel="noopener noreferrer"
        >
          GitHub
          <span aria-hidden="true">↗</span>
        </a>
      </div>

      <div class="contact-note">
        <span>LOCATION</span>
        <strong>Gozo, Malta</strong>
      </div>
    </div>
  </div>
</section>

<style>
  .contact {
    position: relative;
    overflow: hidden;

    border-top: 1px solid rgba(90, 180, 220, 0.08);

    background:
      linear-gradient(
        180deg,
        rgba(2, 11, 24, 0.08),
        rgba(5, 22, 39, 0.34),
        rgba(2, 11, 24, 0.14)
      );
  }

  .contact-atmosphere {
    position: absolute;
    inset: 0;
    pointer-events: none;

    background:
      radial-gradient(
        circle at 20% 36%,
        rgba(85, 214, 158, 0.05),
        transparent 28rem
      ),
      radial-gradient(
        circle at 82% 58%,
        rgba(49, 92, 255, 0.07),
        transparent 34rem
      );
  }

  .contact-container {
    position: relative;
    z-index: 1;

    display: grid;
    grid-template-columns:
      minmax(0, 1.05fr)
      minmax(22rem, 0.95fr);

    gap: clamp(3rem, 7vw, 7rem);
    align-items: center;
  }

  .contact-copy {
    max-width: 46rem;
  }

  .contact-eyebrow {
    margin: 0 0 1.35rem;

    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: var(--font-weight-semibold);

    letter-spacing: 0.22em;
  }

  .contact-copy h2 {
    max-width: 10ch;
    margin: 0;

    font-size: clamp(3.2rem, 5.2vw, 5.6rem);
    line-height: 1;
    letter-spacing: -0.05em;
  }

  .contact-copy h2 span {
    display: block;

    margin-top: 0.08em;

    background:
      linear-gradient(
        92deg,
        var(--aleph-green),
        var(--aleph-cyan) 48%,
        var(--aleph-blue-bright)
      );

    background-clip: text;
    -webkit-background-clip: text;

    color: transparent;
    -webkit-text-fill-color: transparent;
  }

  .contact-lead {
    max-width: 42rem;

    margin: 1.8rem 0 0;

    color: var(--aleph-text-secondary);

    font-size: clamp(1rem, 1.2vw, 1.14rem);
    line-height: 1.75;
  }

  .contact-status {
    display: flex;
    align-items: center;
    gap: 0.65rem;

    margin-top: 2.2rem;

    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.62rem;

    letter-spacing: 0.14em;
  }

  .contact-status-dot {
    width: 0.42rem;
    height: 0.42rem;

    border-radius: 50%;

    background: var(--aleph-green);

    box-shadow:
      0 0 10px rgba(85, 214, 158, 0.5);
  }

  .contact-card {
    position: relative;

    padding: clamp(1.8rem, 3vw, 2.5rem);

    border: 1px solid rgba(90, 180, 220, 0.24);
    border-radius: 1rem;

    background:
      linear-gradient(
        145deg,
        rgba(10, 37, 61, 0.76),
        rgba(3, 18, 34, 0.86)
      );

    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.025),
      0 18px 48px rgba(0, 0, 0, 0.14);
  }

  .contact-card::before {
    content: "";

    position: absolute;
    top: -1px;
    left: 1.6rem;
    right: 1.6rem;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        var(--aleph-green),
        var(--aleph-cyan),
        var(--aleph-blue),
        transparent
      );
  }

  .contact-card-topline {
    display: flex;
    justify-content: space-between;
    align-items: center;

    gap: 1rem;

    margin-bottom: 2rem;

    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.58rem;

    letter-spacing: 0.15em;
  }

  .contact-card-topline span:first-child {
    color: var(--aleph-cyan);
  }

  .contact-email {
    display: flex;
    align-items: center;
    justify-content: space-between;

    gap: 1rem;

    padding-bottom: 1rem;

    border-bottom: 1px solid rgba(90, 180, 220, 0.15);

    color: var(--aleph-text-primary);

    font-size: clamp(1.3rem, 2vw, 1.8rem);
    font-weight: 600;

    letter-spacing: -0.02em;
  }

  .contact-email span {
    color: var(--aleph-cyan);
  }

  .contact-card > p {
    margin: 1.25rem 0 0;

    color: var(--aleph-text-secondary);

    font-size: 0.88rem;
    line-height: 1.65;
  }

  .contact-links {
    display: flex;
    flex-wrap: wrap;

    gap: 0.7rem;

    margin-top: 1.7rem;
  }

  .contact-links a {
    display: inline-flex;
    align-items: center;

    gap: 0.45rem;

    min-height: 2.6rem;

    padding-inline: 0.95rem;

    border: 1px solid var(--aleph-border);
    border-radius: var(--radius-pill);

    color: var(--aleph-text-secondary);

    font-size: 0.74rem;
    font-weight: 600;

    transition:
      color var(--duration-fast) var(--ease-standard),
      border-color var(--duration-fast) var(--ease-standard),
      transform var(--duration-fast) var(--ease-standard);
  }

  .contact-links a:hover {
    color: var(--aleph-text-primary);
    border-color: var(--aleph-border-strong);

    transform: translateY(-1px);
  }

  .contact-note {
    display: flex;
    justify-content: space-between;
    align-items: center;

    gap: 1rem;

    margin-top: 2rem;
    padding-top: 1.2rem;

    border-top: 1px solid rgba(90, 180, 220, 0.11);
  }

  .contact-note span {
    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.56rem;

    letter-spacing: 0.14em;
  }

  .contact-note strong {
    color: var(--aleph-text-primary);

    font-size: 0.82rem;
  }

  @media (max-width: 900px) {
    .contact-container {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .contact-copy h2 {
      font-size: clamp(2.8rem, 12vw, 4.2rem);
    }

    .contact-card {
      padding: 1.5rem 1.25rem;
    }

    .contact-email {
      font-size: 1.15rem;
    }

    .contact-note {
      align-items: flex-start;
      flex-direction: column;
    }
  }
</style>
