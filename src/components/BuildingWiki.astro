<section
  id="wiki"
  class="building-wiki section"
  aria-labelledby="building-wiki-title"
>
  <div class="building-wiki-atmosphere" aria-hidden="true"></div>

  <div class="container-wide building-wiki-container">
    <header class="building-wiki-header">
      <p class="building-wiki-eyebrow">BUILDING IN PUBLIC</p>

      <h2 id="building-wiki-title">
        Engineering should be
        <span class="building-wiki-title-accent">inspectable.</span>
      </h2>

      <p class="building-wiki-intro">
        Aleph Innovation documents architecture, implementation, testing,
        failures and design decisions as its systems evolve. The Wiki is not
        secondary documentation: it is part of the engineering process itself.
      </p>
    </header>

    <div class="building-wiki-grid">
      <article class="building-panel">
        <div class="building-panel-topline">
          <span class="panel-label">BUILDING IN PUBLIC</span>
          <span class="panel-index">01</span>
        </div>

        <h3>Show the work, not only the result.</h3>

        <p>
          Systems become easier to trust when assumptions, constraints,
          architectural choices and validation evidence are visible.
        </p>

        <div class="build-stream" aria-label="Building in public lifecycle">
          <div class="build-stream-item">
            <span class="build-index">A</span>
            <div>
              <strong>Architecture</strong>
              <small>Why the system is structured this way.</small>
            </div>
          </div>

          <div class="build-stream-item">
            <span class="build-index">B</span>
            <div>
              <strong>Implementation</strong>
              <small>How contracts, services and workflows are built.</small>
            </div>
          </div>

          <div class="build-stream-item">
            <span class="build-index">C</span>
            <div>
              <strong>Testing</strong>
              <small>What is verified and how regressions are detected.</small>
            </div>
          </div>

          <div class="build-stream-item">
            <span class="build-index">D</span>
            <div>
              <strong>Failures</strong>
              <small>What did not work and what changed as a result.</small>
            </div>
          </div>

          <div class="build-stream-item">
            <span class="build-index">E</span>
            <div>
              <strong>Lessons learned</strong>
              <small>Reusable engineering conclusions from each iteration.</small>
            </div>
          </div>
        </div>
      </article>

      <article class="wiki-panel">
        <div class="wiki-panel-topline">
          <span class="panel-label">AGENTIC PORTFOLIO · ENGINEERING WIKI</span>
          <span class="panel-index">02</span>
        </div>

        <h3>
          Agentic Portfolio,
          from system architecture to code-level detail.
        </h3>

        <p>
          The Agentic Portfolio Wiki documents the engineering structure of
          Aleph Innovation's first active R&amp;D project: architecture,
          contracts, data types, Python implementation, testing, validation
          and development history.
        </p>

        <div class="wiki-map">
          <a href="/wiki/" class="wiki-map-item">
            <span class="wiki-map-code">00</span>
            <span>
              <strong>System Overview</strong>
              <small>Purpose, boundaries and lifecycle.</small>
            </span>
            <i aria-hidden="true">→</i>
          </a>

          <a href="/wiki/architecture/" class="wiki-map-item">
            <span class="wiki-map-code">01</span>
            <span>
              <strong>Architecture</strong>
              <small>Services, components, flows and ADRs.</small>
            </span>
            <i aria-hidden="true">→</i>
          </a>

          <a href="/wiki/contracts/" class="wiki-map-item">
            <span class="wiki-map-code">02</span>
            <span>
              <strong>Contracts &amp; Data Types</strong>
              <small>Schemas, invariants and provenance.</small>
            </span>
            <i aria-hidden="true">→</i>
          </a>

          <a href="/wiki/python/" class="wiki-map-item">
            <span class="wiki-map-code">03</span>
            <span>
              <strong>Python Implementation</strong>
              <small>Classes, methods and service boundaries.</small>
            </span>
            <i aria-hidden="true">→</i>
          </a>

          <a href="/wiki/testing/" class="wiki-map-item">
            <span class="wiki-map-code">04</span>
            <span>
              <strong>Testing &amp; Validation</strong>
              <small>Regression, semantic and live acceptance gates.</small>
            </span>
            <i aria-hidden="true">→</i>
          </a>

          <a href="/wiki/history/" class="wiki-map-item">
            <span class="wiki-map-code">05</span>
            <span>
              <strong>Development History</strong>
              <small>Milestones, failures and lessons learned.</small>
            </span>
            <i aria-hidden="true">→</i>
          </a>
        </div>

        <div class="wiki-actions">
          <a class="wiki-primary-action" href="/wiki/">
            Explore Agentic Portfolio Wiki
            <span aria-hidden="true">→</span>
          </a>

          <a
            class="wiki-secondary-action"
            href="https://github.com/lupoleo/agentic_portfolio_stage2_5"
            target="_blank"
            rel="noopener noreferrer"
          >
            View public repository
          </a>
        </div>
      </article>
    </div>

    <div class="wiki-foundation">
      <div class="foundation-label">
        <span class="foundation-dot" aria-hidden="true"></span>
        DOCUMENTATION PRINCIPLE
      </div>

      <p>
        A technical claim should be traceable to architecture, code, evidence
        or test results. Documentation should evolve with the system rather
        than describe an idealized version of it.
      </p>
    </div>
  </div>
</section>

<style>
  .building-wiki {
    position: relative;
    overflow: hidden;

    border-bottom: 1px solid rgba(90, 180, 220, 0.08);

    background:
      linear-gradient(
        180deg,
        rgba(2, 11, 24, 0.06),
        rgba(5, 22, 39, 0.28),
        rgba(2, 11, 24, 0.08)
      );
  }

  .building-wiki-atmosphere {
    position: absolute;
    inset: 0;
    pointer-events: none;

    background:
      radial-gradient(
        circle at 18% 36%,
        rgba(85, 214, 158, 0.045),
        transparent 28rem
      ),
      radial-gradient(
        circle at 80% 48%,
        rgba(49, 92, 255, 0.065),
        transparent 34rem
      );
  }

  .building-wiki-container {
    position: relative;
    z-index: 1;
  }

  .building-wiki-header {
    max-width: 72rem;

    margin-bottom: clamp(3.5rem, 6vw, 5.5rem);
  }

  .building-wiki-eyebrow {
    margin: 0 0 1.4rem;

    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: var(--font-weight-semibold);

    letter-spacing: 0.22em;
  }

  .building-wiki-header h2 {
    max-width: 17ch;
    margin: 0;

    font-size: clamp(2.9rem, 4.4vw, 4.8rem);
    line-height: 1.03;
    letter-spacing: -0.045em;
  }

  .building-wiki-title-accent {
    display: block;

    margin-top: 0.08em;

    background:
      linear-gradient(
        92deg,
        var(--aleph-green),
        var(--aleph-cyan) 48%,
        var(--aleph-blue-bright)
      );

    background-clip: text;
    -webkit-background-clip: text;

    color: transparent;
    -webkit-text-fill-color: transparent;
  }

  .building-wiki-intro {
    max-width: 58rem;
    margin: 1.8rem 0 0;

    color: var(--aleph-text-secondary);

    font-size: clamp(1rem, 1.2vw, 1.14rem);
    line-height: 1.75;
  }

  .building-wiki-grid {
    display: grid;
    grid-template-columns:
      minmax(0, 0.92fr)
      minmax(0, 1.08fr);

    gap: clamp(1.4rem, 3vw, 2.2rem);

    align-items: stretch;
  }

  .building-panel,
  .wiki-panel {
    position: relative;

    padding: clamp(1.8rem, 3vw, 2.5rem);

    border: 1px solid rgba(90, 180, 220, 0.24);
    border-radius: 1rem;

    background:
      linear-gradient(
        145deg,
        rgba(10, 37, 61, 0.74),
        rgba(3, 18, 34, 0.84)
      );

    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.025),
      0 16px 44px rgba(0, 0, 0, 0.12);
  }

  .building-panel::before,
  .wiki-panel::before {
    content: "";

    position: absolute;

    top: -1px;
    left: 1.6rem;
    right: 1.6rem;

    height: 1px;
  }

  .building-panel::before {
    background:
      linear-gradient(
        90deg,
        transparent,
        var(--aleph-green),
        var(--aleph-cyan),
        transparent
      );
  }

  .wiki-panel::before {
    background:
      linear-gradient(
        90deg,
        transparent,
        var(--aleph-cyan),
        var(--aleph-blue),
        transparent
      );
  }

  .building-panel-topline,
  .wiki-panel-topline {
    display: flex;
    justify-content: space-between;
    align-items: center;

    gap: 1rem;

    margin-bottom: 2rem;
  }

  .panel-label,
  .panel-index {
    font-family: var(--font-mono);
    font-size: 0.58rem;

    letter-spacing: 0.15em;
  }

  .panel-label {
    color: var(--aleph-green);
  }

  .wiki-panel .panel-label {
    color: var(--aleph-cyan);
    max-width: 28rem;
    line-height: 1.5;
  }

  .panel-index {
    color: var(--aleph-text-muted);
  }

  .building-panel h3,
  .wiki-panel h3 {
    max-width: 24ch;

    margin: 0;

    color: var(--aleph-text-primary);

    font-size: clamp(1.7rem, 2.5vw, 2.5rem);
    line-height: 1.1;
  }

  .building-panel > p,
  .wiki-panel > p {
    max-width: 42rem;

    margin: 1.25rem 0 0;

    color: var(--aleph-text-secondary);

    font-size: 0.94rem;
    line-height: 1.72;
  }

  /* =======================================================
     Building stream
     ======================================================= */

  .build-stream {
    display: grid;

    gap: 0;

    margin-top: 2.2rem;

    border-top: 1px solid rgba(90, 180, 220, 0.12);
  }

  .build-stream-item {
    position: relative;

    display: grid;
    grid-template-columns: 2.5rem minmax(0, 1fr);

    gap: 1rem;

    padding-block: 1rem;

    border-bottom: 1px solid rgba(90, 180, 220, 0.1);
  }

  .build-index {
    display: grid;
    place-items: center;

    width: 1.75rem;
    height: 1.75rem;

    border: 1px solid rgba(85, 214, 158, 0.35);
    border-radius: 50%;

    background: rgba(85, 214, 158, 0.04);

    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.62rem;
  }

  .build-stream-item strong,
  .wiki-map-item strong {
    display: block;

    color: var(--aleph-text-primary);

    font-size: 0.9rem;
    font-weight: 600;
  }

  .build-stream-item small,
  .wiki-map-item small {
    display: block;

    margin-top: 0.25rem;

    color: var(--aleph-text-muted);

    font-size: 0.72rem;
    line-height: 1.45;
  }

  /* =======================================================
     Wiki map
     ======================================================= */

  .wiki-map {
    display: grid;

    margin-top: 2rem;

    border-top: 1px solid rgba(90, 180, 220, 0.12);
  }

  .wiki-map-item {
    display: grid;
    grid-template-columns:
      2.6rem
      minmax(0, 1fr)
      auto;

    align-items: center;

    gap: 1rem;

    padding-block: 0.95rem;

    border-bottom: 1px solid rgba(90, 180, 220, 0.1);

    transition:
      background-color var(--duration-fast) var(--ease-standard),
      padding-left var(--duration-fast) var(--ease-standard);
  }

  .wiki-map-item:hover {
    padding-left: 0.45rem;

    background: rgba(22, 201, 216, 0.025);
  }

  .wiki-map-code {
    color: var(--aleph-cyan);

    font-family: var(--font-mono);
    font-size: 0.63rem;
  }

  .wiki-map-item i {
    color: var(--aleph-blue-bright);

    font-style: normal;
  }

  .wiki-actions {
    display: flex;
    flex-wrap: wrap;

    gap: 0.75rem;

    margin-top: 2rem;
  }

  .wiki-primary-action,
  .wiki-secondary-action {
    display: inline-flex;
    align-items: center;

    gap: 0.6rem;

    min-height: 2.85rem;

    padding-inline: 1rem;

    border-radius: var(--radius-pill);

    font-size: 0.78rem;
    font-weight: 600;
  }

  .wiki-primary-action {
    border: 1px solid rgba(22, 201, 216, 0.42);

    background:
      linear-gradient(
        120deg,
        rgba(22, 201, 216, 0.1),
        rgba(49, 92, 255, 0.07)
      );

    color: var(--aleph-text-primary);
  }

  .wiki-secondary-action {
    border: 1px solid var(--aleph-border);

    color: var(--aleph-text-secondary);
  }

  /* =======================================================
     Documentation principle
     ======================================================= */

  .wiki-foundation {
    display: grid;
    grid-template-columns: auto minmax(0, 1fr);

    gap: clamp(2rem, 4vw, 4rem);

    align-items: center;

    margin-top: 2rem;
    padding: 1.4rem 1.6rem;

    border-top: 1px solid rgba(90, 180, 220, 0.1);
    border-bottom: 1px solid rgba(90, 180, 220, 0.1);
  }

  .foundation-label {
    display: flex;
    align-items: center;

    gap: 0.65rem;

    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.58rem;

    letter-spacing: 0.16em;

    white-space: nowrap;
  }

  .foundation-dot {
    width: 0.4rem;
    height: 0.4rem;

    border-radius: 50%;

    background: var(--aleph-green);

    box-shadow:
      0 0 10px rgba(85, 214, 158, 0.5);
  }

  .wiki-foundation p {
    max-width: 62rem;
    margin: 0;

    color: var(--aleph-text-secondary);

    font-size: 0.82rem;
    line-height: 1.65;
  }

  /* =======================================================
     Responsive
     ======================================================= */

  @media (max-width: 900px) {
    .building-wiki-grid {
      grid-template-columns: 1fr;
    }
  }

  @media (max-width: 640px) {
    .building-wiki-header h2 {
      font-size: clamp(2.7rem, 11vw, 4rem);
    }

    .building-panel,
    .wiki-panel {
      padding: 1.5rem 1.25rem;
    }

    .wiki-map-item {
      grid-template-columns:
        2rem
        minmax(0, 1fr)
        auto;
    }

    .wiki-foundation {
      grid-template-columns: 1fr;
      gap: 1rem;
    }

    .foundation-label {
      white-space: normal;
    }
  }
</style>
