<section
  id="projects"
  class="projects section"
  aria-labelledby="projects-title"
>
  <div class="projects-atmosphere" aria-hidden="true"></div>

  <div class="container-wide projects-container">
    <header class="projects-header">
      <p class="projects-eyebrow">PROJECTS</p>

      <h2 id="projects-title">
        Applied R&amp;D.
        <span class="projects-title-accent">
          One architecture, multiple domains.
        </span>
      </h2>

      <p class="projects-intro">
        Aleph Innovation develops projects around a shared engineering
        paradigm: deterministic software, probabilistic AI, specialized
        agents, explicit validation and observable decision workflows.
      </p>
    </header>

    <div class="projects-grid">
      <article
        id="agentic-portfolio"
        class="project-card project-card-featured"
      >
        <div class="project-topline">
          <span class="project-status project-status-active">
            ACTIVE R&amp;D
          </span>

          <span class="project-number">01</span>
        </div>

        <div class="project-content">
          <p class="project-domain">
            FINANCIAL SYSTEMS · AGENTIC AI
          </p>

          <h3>Agentic Portfolio</h3>

          <p class="project-lead">
            A portfolio-analysis and CIO decision-support framework combining
            quantitative analysis, deterministic risk controls, research,
            semantic scoring and agentic AI.
          </p>

          <p class="project-description">
            The project explores how probabilistic AI can operate inside a
            rigorously controlled investment workflow without replacing
            deterministic computation, portfolio constraints or human
            execution authority.
          </p>

          <div class="project-architecture">
            <span>SCAN</span>
            <i>→</i>
            <span>RESEARCH</span>
            <i>→</i>
            <span>SCORE</span>
            <i>→</i>
            <span>PORTFOLIO FILTER</span>
            <i>→</i>
            <span>SIMULATION</span>
            <i>→</i>
            <span>CIO</span>
          </div>

          <div class="project-tags">
            <span>Python</span>
            <span>Quantitative Analysis</span>
            <span>Portfolio Risk</span>
            <span>LLMs</span>
            <span>Agentic AI</span>
            <span>Validation Gates</span>
          </div>

          <div class="project-actions">
            <a
              class="project-action project-action-primary"
              href="#wiki"
            >
              Explore the architecture
              <span aria-hidden="true">→</span>
            </a>

            <a
              class="project-action project-action-secondary"
              href="https://github.com/lupoleo/agentic_portfolio_stage2_5"
              target="_blank"
              rel="noopener noreferrer"
            >
              GitHub
            </a>
          </div>
        </div>

        <div class="project-visual" aria-hidden="true">
          <div class="project-system-map">
            <div class="system-node system-node-market">
              <span>MARKET</span>
            </div>

            <div class="system-rail system-rail-1"></div>

            <div class="system-node system-node-research">
              <span>RESEARCH</span>
            </div>

            <div class="system-rail system-rail-2"></div>

            <div class="system-node system-node-ai">
              <span>AI</span>
            </div>

            <div class="system-rail system-rail-3"></div>

            <div class="system-node system-node-risk">
              <span>RISK</span>
            </div>

            <div class="system-rail system-rail-4"></div>

            <div class="system-node system-node-cio">
              <span>CIO</span>
            </div>
          </div>

          <div class="project-visual-caption">
            DETERMINISTIC CONTROL
            <span>+</span>
            PROBABILISTIC REASONING
          </div>
        </div>
      </article>

      <div class="concept-heading">
        <span>EXPLORATORY CONCEPTS</span>
        <p>
          Early research directions. These are concepts, not deployed systems.
        </p>
      </div>

      <div class="concept-grid">
        <article class="project-card project-card-concept">
          <div class="project-topline">
            <span class="project-status project-status-concept">
              CONCEPT
            </span>

            <span class="project-number">02</span>
          </div>

          <p class="project-domain">
            ENVIRONMENT · OBSERVATION
          </p>

          <h3>Environmental Monitoring</h3>

          <p>
            Explore how heterogeneous sensor data, public datasets and AI
            interpretation could support local environmental observation and
            anomaly detection.
          </p>

          <div class="concept-footer">
            <span>DATA FUSION</span>
            <span>MONITORING</span>
            <span>ANOMALY DETECTION</span>
          </div>
        </article>

        <article class="project-card project-card-concept">
          <div class="project-topline">
            <span class="project-status project-status-concept">
              CONCEPT
            </span>

            <span class="project-number">03</span>
          </div>

          <p class="project-domain">
            WATER · RESILIENCE
          </p>

          <h3>Water Resources</h3>

          <p>
            Investigate decision-support systems for water availability,
            consumption, storage, desalination and local resource resilience.
          </p>

          <div class="concept-footer">
            <span>RESOURCES</span>
            <span>FORECASTING</span>
            <span>DECISION SUPPORT</span>
          </div>
        </article>

        <article class="project-card project-card-concept">
          <div class="project-topline">
            <span class="project-status project-status-concept">
              CONCEPT
            </span>

            <span class="project-number">04</span>
          </div>

          <p class="project-domain">
            AGRICULTURE · LOCAL SYSTEMS
          </p>

          <h3>Agricultural Intelligence</h3>

          <p>
            Explore AI-assisted analysis of water, weather, soil, crop and
            resource constraints for small-scale Mediterranean agriculture.
          </p>

          <div class="concept-footer">
            <span>AGRICULTURE</span>
            <span>RESOURCE PLANNING</span>
            <span>LOCAL DATA</span>
          </div>
        </article>
      </div>
    </div>
  </div>
</section>

<style>
  .projects {
    position: relative;
    overflow: hidden;

    border-bottom: 1px solid rgba(90, 180, 220, 0.08);
  }

  .projects-atmosphere {
    position: absolute;
    inset: 0;
    pointer-events: none;

    background:
      radial-gradient(
        circle at 16% 35%,
        rgba(85, 214, 158, 0.045),
        transparent 28rem
      ),
      radial-gradient(
        circle at 82% 44%,
        rgba(49, 92, 255, 0.06),
        transparent 32rem
      );
  }

  .projects-container {
    position: relative;
    z-index: 1;
  }

  .projects-header {
    max-width: 72rem;
    margin-bottom: clamp(3.5rem, 6vw, 5.5rem);
  }

  .projects-eyebrow {
    margin: 0 0 1.4rem;

    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.68rem;
    font-weight: var(--font-weight-semibold);

    letter-spacing: 0.22em;
  }

  .projects-header h2 {
    max-width: 18ch;
    margin: 0;

    font-size: clamp(2.9rem, 4.4vw, 4.8rem);
    line-height: 1.03;
    letter-spacing: -0.045em;
  }

  .projects-title-accent {
    display: block;

    margin-top: 0.08em;

    background:
      linear-gradient(
        92deg,
        var(--aleph-green),
        var(--aleph-cyan) 48%,
        var(--aleph-blue-bright)
      );

    background-clip: text;
    -webkit-background-clip: text;

    color: transparent;
    -webkit-text-fill-color: transparent;
  }

  .projects-intro {
    max-width: 57rem;
    margin-top: 1.8rem;

    color: var(--aleph-text-secondary);

    font-size: clamp(1rem, 1.2vw, 1.14rem);
    line-height: 1.75;
  }

  .projects-grid {
    display: grid;
    gap: 3.25rem;
  }

  /* =======================================================
     Featured project
     ======================================================= */

  .project-card {
    position: relative;

    border: 1px solid rgba(90, 180, 220, 0.24);
    border-radius: 1.1rem;

    background:
      linear-gradient(
        145deg,
        rgba(10, 37, 61, 0.76),
        rgba(3, 18, 34, 0.84)
      );
  }

  .project-card-featured {
    display: grid;
    grid-template-columns:
      minmax(0, 1.12fr)
      minmax(22rem, 0.88fr);

    gap: clamp(2.5rem, 5vw, 5rem);

    padding: clamp(2rem, 4vw, 3.4rem);

    box-shadow:
      inset 0 1px rgba(255, 255, 255, 0.025),
      0 20px 60px rgba(0, 0, 0, 0.14);
  }

  .project-card-featured::before {
    content: "";

    position: absolute;

    top: -1px;
    left: 2rem;
    right: 2rem;

    height: 1px;

    background:
      linear-gradient(
        90deg,
        transparent,
        var(--aleph-green),
        var(--aleph-cyan),
        var(--aleph-blue),
        transparent
      );
  }

  .project-topline {
    position: absolute;

    top: 1.25rem;
    left: 1.4rem;
    right: 1.4rem;

    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .project-status {
    display: inline-flex;
    align-items: center;

    padding: 0.34rem 0.58rem;

    border-radius: var(--radius-pill);

    font-family: var(--font-mono);
    font-size: 0.55rem;
    font-weight: 600;

    letter-spacing: 0.12em;
  }

  .project-status-active {
    border: 1px solid rgba(85, 214, 158, 0.35);

    background: rgba(85, 214, 158, 0.07);

    color: var(--aleph-green);
  }

  .project-status-concept {
    border: 1px solid rgba(90, 180, 220, 0.2);

    background: rgba(7, 27, 48, 0.6);

    color: var(--aleph-text-muted);
  }

  .project-number {
    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.62rem;

    letter-spacing: 0.14em;
  }

  .project-content {
    padding-top: 2.2rem;
  }

  .project-domain {
    margin: 0 0 1rem;

    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.6rem;

    letter-spacing: 0.16em;
  }

  .project-card h3 {
    margin: 0;

    color: var(--aleph-text-primary);

    font-size: clamp(1.8rem, 2.8vw, 3rem);
    line-height: 1.08;
  }

  .project-lead {
    max-width: 45rem;

    margin: 1.35rem 0 0;

    color: var(--aleph-text-primary);

    font-size: clamp(1rem, 1.28vw, 1.18rem);
    line-height: 1.68;
  }

  .project-description {
    max-width: 44rem;

    margin: 1.15rem 0 0;

    color: var(--aleph-text-secondary);

    font-size: 0.95rem;
    line-height: 1.7;
  }

  .project-architecture {
    display: flex;
    flex-wrap: wrap;
    align-items: center;

    gap: 0.45rem;

    margin-top: 1.8rem;

    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.55rem;

    letter-spacing: 0.08em;
  }

  .project-architecture span {
    padding: 0.34rem 0.52rem;

    border: 1px solid rgba(90, 180, 220, 0.14);
    border-radius: 0.35rem;

    background: rgba(2, 11, 24, 0.34);
  }

  .project-architecture i {
    color: var(--aleph-cyan);

    font-style: normal;
  }

  .project-tags {
    display: flex;
    flex-wrap: wrap;

    gap: 0.5rem;

    margin-top: 1.6rem;
  }

  .project-tags span,
  .concept-footer span {
    padding: 0.42rem 0.65rem;

    border: 1px solid rgba(90, 180, 220, 0.25);
    border-radius: var(--radius-pill);

    background: rgba(5, 22, 39, 0.66);

    color: var(--aleph-text-secondary);

    font-size: 0.62rem;
  }

  .project-actions {
    display: flex;
    flex-wrap: wrap;

    gap: 0.75rem;

    margin-top: 2rem;
  }

  .project-action {
    display: inline-flex;
    align-items: center;

    gap: 0.6rem;

    min-height: 2.8rem;

    padding-inline: 1rem;

    border-radius: var(--radius-pill);

    font-size: 0.78rem;
    font-weight: 600;
  }

  .project-action-primary {
    border: 1px solid rgba(85, 214, 158, 0.42);

    background:
      linear-gradient(
        120deg,
        rgba(85, 214, 158, 0.13),
        rgba(22, 201, 216, 0.07)
      );

    color: var(--aleph-text-primary);
  }

  .project-action-secondary {
    border: 1px solid var(--aleph-border);

    color: var(--aleph-text-secondary);
  }

  /* =======================================================
     Project visual
     ======================================================= */

  .project-visual {
    position: relative;

    display: flex;
    flex-direction: column;

    align-items: center;
    justify-content: center;

    min-height: 28rem;

    padding-top: 2rem;
  }

  .project-system-map {
    position: relative;

    display: flex;
    align-items: center;
    justify-content: center;

    width: min(100%, 32rem);
    min-height: 16rem;
  }

  .system-node {
    position: absolute;

    display: grid;
    place-items: center;

    width: 4.7rem;
    height: 4.7rem;

    border: 1px solid rgba(22, 201, 216, 0.34);
    border-radius: 50%;

    background:
      radial-gradient(
        circle,
        rgba(22, 201, 216, 0.1),
        rgba(4, 20, 38, 0.8)
      );

    color: var(--aleph-text-primary);

    font-family: var(--font-mono);
    font-size: 0.55rem;

    letter-spacing: 0.11em;

    box-shadow:
      0 0 22px rgba(22, 201, 216, 0.04);
  }

  .system-node-market {
    left: 0;
    top: 5.5rem;
  }

  .system-node-research {
    left: 23%;
    top: 1.2rem;
  }

  .system-node-ai {
    left: 46%;
    top: 6rem;

    border-color: rgba(49, 92, 255, 0.5);
  }

  .system-node-risk {
    right: 8%;
    top: 1.8rem;

    border-color: rgba(85, 214, 158, 0.42);
  }

  .system-node-cio {
    right: 0;
    bottom: 0.8rem;

    border-color: rgba(85, 214, 158, 0.6);

    box-shadow:
      0 0 26px rgba(85, 214, 158, 0.08);
  }

  .system-rail {
    position: absolute;

    height: 1px;

    transform-origin: left center;

    background:
      linear-gradient(
        90deg,
        rgba(85, 214, 158, 0.28),
        rgba(22, 201, 216, 0.52),
        rgba(49, 92, 255, 0.28)
      );
  }

  .system-rail-1 {
    width: 7rem;

    left: 3.8rem;
    top: 7rem;

    transform: rotate(-29deg);
  }

  .system-rail-2 {
    width: 7rem;

    left: 10.8rem;
    top: 4.6rem;

    transform: rotate(31deg);
  }

  .system-rail-3 {
    width: 7rem;

    left: 17rem;
    top: 7.8rem;

    transform: rotate(-32deg);
  }

  .system-rail-4 {
    width: 6.5rem;

    right: 1.8rem;
    top: 6rem;

    transform: rotate(51deg);
  }

  .project-visual-caption {
    margin-top: 1.8rem;

    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.55rem;

    letter-spacing: 0.12em;

    text-align: center;
  }

  .project-visual-caption span {
    margin-inline: 0.55rem;

    color: var(--aleph-cyan);
  }

  /* =======================================================
     Concepts
     ======================================================= */

  .concept-heading {
    display: flex;
    justify-content: space-between;
    align-items: end;

    gap: 2rem;

    border-bottom: 1px solid rgba(90, 180, 220, 0.1);

    padding-bottom: 1rem;
  }

  .concept-heading span {
    color: var(--aleph-green);

    font-family: var(--font-mono);
    font-size: 0.62rem;

    letter-spacing: 0.17em;
  }

  .concept-heading p {
    margin: 0;

    color: var(--aleph-text-muted);

    font-size: 0.8rem;
  }

  .concept-grid {
    display: grid;
    grid-template-columns: repeat(3, minmax(0, 1fr));

    gap: 1.25rem;
  }

  .project-card-concept {
    display: flex;
    flex-direction: column;

    min-height: 21rem;

    padding: 3.8rem 1.6rem 1.6rem;
  }

  .project-card-concept h3 {
    margin-top: 0.7rem;

    font-size: clamp(1.35rem, 1.8vw, 1.7rem);
  }

  .project-card-concept > p:not(.project-domain) {
    margin-top: 1.1rem;

    color: var(--aleph-text-secondary);

    font-size: 0.86rem;
    line-height: 1.65;
  }

  .concept-footer {
    display: flex;
    flex-wrap: wrap;

    gap: 0.4rem;

    margin-top: auto;
    padding-top: 1.5rem;
  }

  .concept-footer span {
    font-family: var(--font-mono);
    font-size: 0.52rem;

    letter-spacing: 0.05em;
  }

  /* =======================================================
     Responsive
     ======================================================= */

  @media (max-width: 980px) {
    .project-card-featured {
      grid-template-columns: 1fr;
    }

    .project-visual {
      min-height: 24rem;
    }

    .concept-grid {
      grid-template-columns: 1fr;
    }

    .project-card-concept {
      min-height: auto;
    }
  }

  @media (max-width: 640px) {
    .concept-heading {
      align-items: start;
      flex-direction: column;
    }

    .project-card-featured {
      padding: 2rem 1.3rem;
    }

    .project-system-map {
      transform: scale(0.86);
    }
  }
</style>