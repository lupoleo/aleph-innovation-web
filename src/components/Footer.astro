<footer class="site-footer">
  <div class="container-wide footer-inner">
    <div class="footer-brand">
      <a class="footer-logo" href="/" aria-label="Aleph Innovation home">
        <span class="footer-mark" aria-hidden="true">
          <span></span>
          <span></span>
        </span>

        <span class="footer-name">
          <strong>ALEPH</strong>
          <small>INNOVATION</small>
        </span>
      </a>

      <p>
        Engineering Agentic AI systems from Gozo, Malta.
      </p>
    </div>

    <nav class="footer-nav" aria-label="Footer navigation">
      <a href="#paradigm">Paradigm</a>
      <a href="#projects">Projects</a>
      <a href="#agentic-portfolio">Agentic Portfolio</a>
      <a href="#wiki">Wiki</a>
      <a href="#about">About</a>
      <a href="#contact">Contact</a>
    </nav>

    <div class="footer-meta">
      <p>
        Pre-incorporation R&amp;D venture · Gozo, Malta
      </p>

      <div class="footer-meta-links">
        <a href="mailto:info@alephinnovation.com">
          info@alephinnovation.com
        </a>

        <span aria-hidden="true">·</span>

        <a
          href="https://www.linkedin.com/company/alephinnovation/"
          target="_blank"
          rel="noopener noreferrer"
        >
          LinkedIn
        </a>

        <span aria-hidden="true">·</span>

        <a
          href="https://github.com/lupoleo/agentic_portfolio_stage2_5"
          target="_blank"
          rel="noopener noreferrer"
        >
          GitHub
        </a>
      </div>
    </div>
  </div>

  <div class="container-wide footer-bottom">
    <span>© 2026 Aleph Innovation</span>

    <span>
      Building in public · Engineering first
    </span>
  </div>
</footer>

<style>
  .site-footer {
    position: relative;

    border-top: 1px solid rgba(90, 180, 220, 0.1);

    background:
      linear-gradient(
        180deg,
        rgba(2, 11, 24, 0.72),
        rgba(2, 11, 24, 0.94)
      );
  }

  .footer-inner {
    display: grid;
    grid-template-columns:
      minmax(0, 1.05fr)
      auto
      minmax(0, 0.95fr);

    gap: clamp(2rem, 5vw, 5rem);

    align-items: start;

    padding-block: clamp(2.8rem, 5vw, 4rem);
  }

  .footer-brand {
    max-width: 22rem;
  }

  .footer-logo {
    display: inline-flex;
    align-items: center;

    gap: 0.8rem;
  }

  .footer-mark {
    position: relative;

    width: 1.8rem;
    height: 1.8rem;
  }

  .footer-mark span {
    position: absolute;
    display: block;

    border-radius: 0.3rem;
  }

  .footer-mark span:first-child {
    width: 0.64rem;
    height: 1.7rem;

    left: 0.2rem;
    top: 0.02rem;

    transform: rotate(28deg);

    background:
      linear-gradient(
        180deg,
        var(--aleph-green),
        var(--aleph-cyan)
      );
  }

  .footer-mark span:last-child {
    width: 0.6rem;
    height: 1.2rem;

    right: 0.15rem;
    bottom: 0.04rem;

    transform: rotate(-42deg);

    background:
      linear-gradient(
        180deg,
        var(--aleph-blue-bright),
        var(--aleph-blue)
      );
  }

  .footer-name {
    display: grid;
    gap: 0.04rem;

    line-height: 1;
  }

  .footer-name strong {
    color: var(--aleph-text-primary);

    font-size: 0.86rem;
    letter-spacing: 0.26em;
  }

  .footer-name small {
    color: var(--aleph-green);

    font-size: 0.56rem;
    font-weight: 600;

    letter-spacing: 0.22em;
  }

  .footer-brand p {
    margin: 1rem 0 0;

    color: var(--aleph-text-muted);

    font-size: 0.78rem;
    line-height: 1.6;
  }

  .footer-nav {
    display: grid;
    gap: 0.65rem;
  }

  .footer-nav a {
    color: var(--aleph-text-secondary);

    font-size: 0.76rem;

    transition:
      color var(--duration-fast) var(--ease-standard);
  }

  .footer-nav a:hover {
    color: var(--aleph-text-primary);
  }

  .footer-meta {
    justify-self: end;

    max-width: 26rem;
  }

  .footer-meta p {
    margin: 0;

    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.58rem;

    letter-spacing: 0.11em;

    text-align: right;
  }

  .footer-meta-links {
    display: flex;
    flex-wrap: wrap;
    justify-content: flex-end;

    gap: 0.55rem;

    margin-top: 1rem;

    color: var(--aleph-text-muted);

    font-size: 0.72rem;
  }

  .footer-meta-links a {
    color: var(--aleph-text-secondary);
  }

  .footer-meta-links a:hover {
    color: var(--aleph-cyan);
  }

  .footer-bottom {
    display: flex;
    justify-content: space-between;
    align-items: center;

    gap: 1rem;

    padding-block: 1rem;

    border-top: 1px solid rgba(90, 180, 220, 0.08);

    color: var(--aleph-text-muted);

    font-family: var(--font-mono);
    font-size: 0.55rem;

    letter-spacing: 0.09em;
  }

  @media (max-width: 820px) {
    .footer-inner {
      grid-template-columns: 1fr 1fr;
    }

    .footer-meta {
      grid-column: 1 / -1;

      justify-self: start;
    }

    .footer-meta p {
      text-align: left;
    }

    .footer-meta-links {
      justify-content: flex-start;
    }
  }

  @media (max-width: 560px) {
    .footer-inner {
      grid-template-columns: 1fr;
    }

    .footer-bottom {
      align-items: flex-start;
      flex-direction: column;
    }
  }
</style>
